import { FC } from 'react';
import { ShippingForm } from './ShippingForm';

export const App: FC = () => {
  return (
    <div>
      <h1>React hook form sample</h1>
      <ShippingForm />
    </div>
  );
};
